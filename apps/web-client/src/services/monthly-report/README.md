# Monthly Report Service

## Overview

Generates and displays monthly financial reports.

## Architecture

- `procedures/`: report generation and retrieval.
- `components/`: report UI.
- `schema/` and `types/`: validations and types.
- `utils/`: report helpers.

## User flow

1. User requests a monthly report.
2. Server generates or fetches report data.
3. UI renders report results.
